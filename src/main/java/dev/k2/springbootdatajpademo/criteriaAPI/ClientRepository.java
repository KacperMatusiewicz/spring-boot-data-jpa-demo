package dev.k2.springbootdatajpademo.criteriaAPI;

public class ClientRepository {
/*
        CriteriaBuilder criteriaBuilder = em.getCriteriaBuilder();
        CriteriaQuery<Pizza> query = criteriaBuilder.createQuery(Pizza.class);
        Root<Pizza> root = query.from(Pizza.class);
        query.select(root).where(criteriaBuilder.equal(root.get("slices"), slices));
        return em.createQuery(query).getResultList();*/
}
